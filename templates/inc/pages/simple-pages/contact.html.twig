{% include 'inc/parts/head.html.twig' %}
{% include 'inc/parts/nav.html.twig' %}

{% for message in app.flashes('success') %}
    <div class="alert alert-success">
        {{ message }}
    </div>
{% endfor %}
{% for message in app.flashes('error') %}
    <div class="alert alert-error">
        {{ message }}
    </div>
{% endfor %}

<div class="atypikhouse-container">
    <div class="page-title">
        <h1 class="text-uppercase c1 size20 ff-r">{{ title }}</h1>
    </div>
    <p>Les champs avec un indice (*) sont à renseigner obligatoirement.</p>
    <div class="contact-form grid-lg-2 my-4 border-radius-form box-shadow">
        <div>
            <img src="assets/pictures/pic-ath3.jpg" alt="Image du formulaire de contact"/>
        </div>
        <form class="p-2" method="POST">
            <div class="form-row">
                <div class="form-group bloc-lg-6">
                    <label for="first-name">Prénom *</label>
                    <input type="text" placeholder="Prénom *" name="first-name" id="first-name" required>
                </div>
                <div class="form-group bloc-lg-6">
                    <label for="last-name">Nom *</label>
                    <input type="text" placeholder="Nom *" name="last-name" id="last-name" required>
                </div>
            </div>

            <div class="form-row">
                <div class="form-group bloc-lg-6">
                    <label for="contact-phone">Téléphone</label>
                    <input type="tel" placeholder="Téléphone" name="contact-phone" id="contact-phone">
                </div>
                <div class="form-group bloc-lg-6">
                    <label for="contact-email">E-mail *</label>
                    <input type="email" placeholder="E-mail *" name="contact-email" id="contact-email" required>
                </div>
            </div>
            
            <div class="form-group">
                <label for="contact-object">Objet *</label>
                <input type="text" placeholder="Objet *" name="contact-object" id="contact-object" required>
            </div>
            <div class="form-group">
                <label for="contact-message">Message</label>
                <textarea placeholder="Message" name="contact-message" id="contact-message"></textarea>
            </div>

            <input type="submit" name="btn-contact" class="ml-auto click click-success" value="Envoyer ma demande">
        </form>
    </div>
</div>


{% include 'inc/parts/footer.html.twig' %}